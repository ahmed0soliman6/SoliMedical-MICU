rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for initial deployment and easy setup
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
